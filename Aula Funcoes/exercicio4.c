/*Crie a função imprime() que recebe um vetor de inteiros e o tamanho do vetor e imprime os elementos
desse vetor na saída padrão separando-os por vírgula. Em seguida, use a função ler_inteiros() para ler os
valores do vetor e use a função imprime().*/
#include <stdio.h>

int imprime(int vetor[], int tamanho){
    
}