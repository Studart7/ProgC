/*Escreva um programa com as seguintes funções:
• função que lê uma matriz -> recebe uma matriz, a quantidade de linhas e a
quantidade de colunas;
• função que soma os elementos acima da diagonal principal, retornando o resultado
-> recebe uma matriz, a quantidade de linhas e a quantidade de colunas;
• função que soma os elementos abaixo da diagonal principal, retornando o
resultado -> recebe uma matriz, a quantidade de linhas e a quantidade de colunas;*/
#include <stdio.h>

void ler_matriz(){

    
}
int soma_acima(){

    return resultado;
}
int soma_abaixo(){

    return resultado;
}
